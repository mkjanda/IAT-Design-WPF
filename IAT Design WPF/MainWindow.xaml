<Window x:Class="IAT_Design_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:IAT.Views.Controls;assembly=IAT.Views"
        mc:Ignorable="d"
        Title="IAT Designer - My First Test"
        Height="720" Width="1280"
        Background="#1E1E1E"
        Foreground="#E0E0E0"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Top Tab Bar -->
    
        <TabControl Grid.Row="0" Background="#252526" BorderThickness="0" 
                    SelectedIndex="1" Height="30"
                    Style="{StaticResource ModernTabControlStyle}">
            <TabItem Header="Blocks" FontSize="18" Content="Blocks" Width="70" Foreground="#F7C46C" />

            <TabItem Header="Stimuli" IsSelected="True" FontSize="18" Content="Stimuli" Width="70" Foreground="#F7C46C"/>
            <TabItem Header="Trials" FontSize="18" Content="Trials" Width="70" Foreground="#F7C46C"/>
            <TabItem Header="Deploy" FontSize="18" Content="Deploy" Width="70" Foreground="#F7C46C"/>
        </TabControl>
     
            <controls:ErrorBannerControl x:Name="ErrorBanner" 
                              Grid.Row="1"
                              RenderTransformOrigin="0.5,0"
                              RenderTransform="{StaticResource TranslateTransformZero}"/>

        <controls:BlockEditView x:Name="BlockEditView" DataContext="{Binding BlockEditViewModel}" Grid.Row="2" />



    </Grid>
</Window>